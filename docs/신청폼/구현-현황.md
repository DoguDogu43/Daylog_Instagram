# 신청폼 구현 현황

## 현재 상태

React, TypeScript와 Vite 기반 초기 신청 경험을 구현했습니다. 배포 대상은 Vercel이며, 최종 신청과 퍼널 이벤트는 동일 출처 Serverless Function을 거쳐 기존 통합 Google Apps Script로 전달하도록 구성했습니다.

신청 경험은 `짧게 선택 → LIFE NOTE 자동 작성 → 최소 연락 정보 입력 → 신청 완료` 흐름으로 구성하며 전체 약 3분을 목표로 합니다.

구현 경로:

```text
/Users/won-young/Documents/ChatGPT/데이로그/application-form
```

## 브랜드 경험

참고 사이트의 음악·레코딩 세계관을 복제하지 않고 다음 구조적 장점만 데이로그에 맞게 해석했습니다.

- 전체 화면에서 브랜드 문장을 강하게 제시하는 인트로
- 현재 상태를 알려주는 짧은 영문 레이블과 단계 번호
- 한 번에 하나의 질문에 집중하는 흐름
- 큰 CTA와 다음 행동을 설명하는 보조 문구
- 선택이 누적되며 빈 줄이 채워지는 `나의 하루 노트`

화면의 중심 이미지는 제거했습니다. 첫 화면부터 질문, 요약, 연락 신청과 완료까지 종이, 가로줄, 체크, 메모와 도장으로 이어지는 노트형 디자인을 사용합니다. 코치 사진이나 하단 인물 이미지를 렌더링하지 않습니다.

## 화면 흐름

```text
인트로
  → 하루 리듬
  → 편안한 시간·힘든 시간
  → 과거의 지속 방식
  → 바꾸고 싶은 생활 영역
  → 함께하고 싶은 방식
  → 나의 하루 노트 요약
  → 연락·일정 정보
  → 신청 완료
```

## 구현된 질문

| 단계 | 질문 | 입력 방식 |
| --- | --- | --- |
| 1 | 요즘 하루는 어떤 리듬으로 흘러가나요? | 단일 선택 |
| 2 | 가장 편안한 시간과 가장 힘든 시간은 언제인가요? | 미선택 상태가 있는 0~23시 선택 목록 |
| 3 | 오래 이어가 본 일이 있나요? | 단일 선택 |
| 4 | 가장 먼저 바꾸고 싶은 곳은 어디인가요? | 1~3개 선택 후 우선 영역 지정 |
| 5 | 어떤 방식이면 더 오래 이어갈 수 있을까요? | 단일 선택 |

## 연락 신청 항목

- 필수: 이름 또는 호칭
- 필수: 연락 방법과 연락처
- 필수: 개인정보 수집·이용 동의
- 선택: 희망 요일
- 선택: 희망 시간대
- 선택: 추가 이야기 500자 이내
- 사용자에게 보이지 않는 스팸 방지 필드

선택 항목 세 개는 기본 화면에서 접어두며 `일정과 메모 추가하기`를 눌렀을 때만 표시합니다. 선택 항목을 비워도 신청할 수 있습니다.

## 데이터 처리

- 생활 탐색 답변만 `sessionStorage`에 임시 저장합니다.
- 이름과 연락처는 브라우저 저장소에 저장하지 않습니다.
- 제출 재시도 시 같은 접수번호를 사용합니다.
- 퍼널 이벤트에는 질문 답변과 개인정보를 넣지 않습니다.
- Vercel API에서 허용값, 길이, 연락처 형식과 식별자 형식을 재검증합니다.
- Apps Script 비밀값과 URL은 서버 환경변수에서만 읽습니다.
- Google Sheets에는 접수번호와 이벤트 ID 기준 중복 방지를 적용합니다.
- 완성 노트 확인 퍼널 이벤트는 `life_note_viewed`를 사용합니다.

## 반응형 UI

- 1280×720 화면에서 첫 화면 CTA가 스크롤 전에 보입니다.
- 600~839px에서는 작성된 항목과 현재 기록을 중심으로 노트 높이를 줄입니다.
- 599px 이하에서는 헤더를 축소하고 인트로의 장식용 노트와 요약의 중복 노트 화면을 숨깁니다.
- 질문 화면에서는 선택 답변 노트 미리보기를 렌더링하지 않고 완성 노트는 최종 요약에서만 제공합니다.
- 599px 이하 질문 화면에서는 일반 선택 카드를 읽기 쉬운 1열, 생활 영역을 2열로 배치합니다.
- 700px 이하의 낮은 화면에서는 생활 영역을 제목 중심 3열로 바꾸고, 연락 화면의 선택 일정·메모와 긴 동의 설명을 숨깁니다.
- 390×844와 320×568 브라우저 뷰포트에서 인트로, 질문, 요약과 기본 연락 화면이 세로 스크롤 없이 표시되는 것을 확인했습니다.
- 선택 상태는 옅은 Yellow 배경, Deep Navy 테두리·체크와 얇은 Yellow 표시로 구분합니다.
- 데스크톱에서는 작성된 노트 문장을 14px 이상으로 표시하고 모바일에서는 현재 줄만 간결하게 축소합니다.

## 코드 구조

```text
application-form/
├── api/
│   ├── _shared.ts
│   └── daylog/
│       ├── application.ts
│       └── track.ts
├── google-apps-script/
│   └── DaylogLifeSession.gs
├── src/
│   ├── App.tsx
│   ├── App.css
│   ├── RoutineNotebook.tsx
│   ├── data.ts
│   ├── index.css
│   └── types.ts
├── .env.example
├── index.html
└── package.json
```

## 남은 운영 작업

- 실제 통합 Apps Script의 `Main.gs`에 데이로그 비밀키 매핑과 분기 추가
- `DaylogLifeSession.gs`를 실제 Apps Script 프로젝트에 추가
- Apps Script 스크립트 속성에 `DAYLOG_LIFE_SESSION_SECRET` 등록
- Vercel 환경변수 등록
- Apps Script와 Vercel 재배포
- 테스트 신청 후 데이로그 전용 세 탭 생성 및 기록 확인
- 공개 개인정보 처리방침의 보관 기간과 파기 절차 확정
